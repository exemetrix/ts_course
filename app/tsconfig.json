{
    "extends": "../tsconfig.base", // .json extension is not needed
    "compilerOptions": {
        "removeComments": true
    },
    "include": [
        "./**/*" // Begin searching in the current dir; search recursively in all subdirs
    ]
}