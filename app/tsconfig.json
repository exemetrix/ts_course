{
    "extends": "../tsconfig.base", // Extend the main config file ().json extension is not needed)
    "compilerOptions": {
        "removeComments": true
    },
    "include": [
        "./**/*" // Begin searching for ts files in the current dir; search recursively in all subdirs
    ]
}